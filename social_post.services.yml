services:
  social_post.user_manager:
    class: Drupal\social_post\SocialPostManager
    arguments:
      - '@logger.factory'
      - '@entity_type.manager'
      - '@entity.query'
      - '@current_user'
